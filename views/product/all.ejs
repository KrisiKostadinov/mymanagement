<h2>All products in my company</h2>
<ul>
    <% products.forEach(product => { %>
        <li>
            <a href="/product/<%=product._id%>"><%= product.name %></a>
            <img src="<%= product.imageUrl %>">
        </li>
    <% }) %>
</ul>